<configuration>
    <statusListener class="ch.qos.logback.core.status.NopStatusListener" /> <!-- disable internal logback logging -->
    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/ecommerce.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

<!--    log to db-->
    <appender name="DB" class="ch.qos.logback.classic.db.DBAppender">
        <connectionSource class="ch.qos.logback.core.db.DataSourceConnectionSource">
            <dataSource class="org.postgresql.ds.PGSimpleDataSource">
                <serverName>localhost</serverName>
                <portNumber>5555</portNumber>
                <databaseName>ecommerce_servlet</databaseName>
                <user>postgres</user>
                <password>123456@Ab</password>
                <currentSchema>logging</currentSchema>
            </dataSource>
        </connectionSource>
    </appender>



    <!-- DEBUG < INFO < WARN < ERROR -->
    <root level="debug">  
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="DB" />
    </root>
</configuration>